package myCheckers;

public class King
{

//    // did piece move to King's row? Change type to King
//    if (piece.getType().moveDir == 1 && newY == 0) {
//        piece = makePiece(PieceType.RED_KING, x, y);
//    } else if (piece.getType().moveDir == -1 && newY == 7) {
//        piece = makePiece(PieceType.WHITE_KING, x, y);
//    }

}
